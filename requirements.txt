# Shopping Truth Agent — Dependencies
# Install: pip install -r requirements.txt

# ── Core (required) ──
httpx>=0.24.0           # Async HTTP client for all API calls

# ── ML / Analysis (optional — agent works without these) ──
numpy>=1.24.0           # Numerical analysis
# xgboost>=1.7.0        # ML fake review detection (optional, falls back to keyword)
# scikit-learn>=1.3.0   # ML utilities (optional)
# scipy>=1.10.0         # Statistical analysis (optional)
# statsmodels>=0.14.0   # ARIMA price prediction (optional)

# Python >= 3.10 required
