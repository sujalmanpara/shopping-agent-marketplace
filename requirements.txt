# Shopping Truth Agent — Production Dependencies
# Phase 2: API-first architecture (no browser scraping)

# Core
httpx>=0.25.0             # Async HTTP client for all API calls
numpy>=1.24.0             # Numerical operations
scipy>=1.11.0             # Scientific computing (ARIMA, sparse matrices)
joblib>=1.3.0             # ML model serialization

# ML Models
xgboost>=2.0.0            # Fake review detection model
scikit-learn>=1.3.0       # ML pipeline + StandardScaler
pandas>=2.0.0             # Feature extraction DataFrame support
statsmodels>=0.14.0       # ARIMA time-series prediction

# Optional: API Clients (enable with credentials)
# praw>=7.8.1             # Reddit API (uncomment if using PRAW)
